{
  "name": "smoke-runner",
  "version": "2.0.0",
  "description": "Drives the smoke scenario through the shipped `mauto` CLI (no parallel mobile-mcp client). See run-smoke.mjs header for what it proves.",
  "type": "module",
  "private": true,
  "scripts": {
    "smoke": "node run-smoke.mjs"
  }
}
